name: CustomBlockGUI
version: '1.2.0'
main: com.customblocks.CustomBlockPlugin
api-version: '1.21'
author: CustomBlocks
description: DiscoveryLab 15 machines with recipes + per-block GUI (crusher/drill/generator) - new way no Oraxen CMD 1001-1015
prefix: CustomBlock

commands:
  givecustomblock:
    description: Give DiscoveryLab custom block (different texture per type)
    usage: /<command> <type> [player] [amount] | /<command> * for all
    permission: customblock.give
    permission-message: You don't have permission!
  customblock:
    description: Main command - /cb types|list|info
    usage: /<command>
    aliases: [cb]

permissions:
  customblock.give:
    description: Allow giving custom block
    default: op
  customblock.*:
    description: All permissions
    default: op
    children:
      customblock.give: true
